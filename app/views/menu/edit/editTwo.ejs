<!doctype html>
<html>
<head>
	<title><%= title %></title>
</head>
<body>
	<h1><%= title %></h1>
	<% include ../../messages %>
	<form  action='http://www.fullab.top/wechat-back/menu/insert/2' method='post'>
		<p>
			<label for='id'>
			ID:
				<input readonly name='_id' value='<%=subButton._id%>'>
			</label>
		</p>
		<p>
			<label for='parentName'>
				ParentName:
					<input readonly name='parentName' value='<%=subButton.parentName%>'>
				</select>
			</label>
		</p>
		<p>
			<label for='name'>
				Name:
				<input type='text' id='name' name='name' value='<%= subButton.name%>'/>
			</label>
		</p>
		<p>
			<label for='type'>
				Type:
				<select id='select' name='type'>
					<option value='click'>点击推事件</option>
					<option value='view'>跳转URL</option>
					<option value='scancode_push'>扫码推事件</option>
					<option value='scancode_waitmsg'>扫码推事件且弹出“消息接收中”提示框</option>
					<option value='pic_sysphoto'>弹出系统拍照发图</option>
					<option value='pic_photo_or_album'>弹出拍照或者相册发图</option>
					<option value='pic_weixin'>弹出微信相册发图器</option>
					<option value='location_select'>弹出地理位置选择器</option>
					<option value='media_id'>下发消息（除文本消息）</option>
					<option value='view_limited'>跳转图文消息URL</option>
				</select>
			</label>
		</p>
		<p>
			<label for='key'>
				Key:
				<input type='text' id='key' name='key' value='<%=subButton.key%>'/>
			</label>
		</p>
		<p>
			<label for='url'>
				Url:
				<input type='text' id='url' name='url' value='<%=subButton.url%>'/>
			</label>
		</p>
		<p>
			<label for='mediaId'>
				Media_id:
				<input type='text' id='mediaId' name='mediaId' value=<%=subButton.media_id%>>
			</label>
		</p>
		<p>
			<input type='submit'/>
		</p>
	</form>
	<script type='text/javascript'>
		var childs = document.getElementById('select').children;
		for(var i=0;i<childs.length;i++){
			if('<%=subButton.type%>' == childs[i].value){
				childs[i].selected = true;
			}
		}
	</script>
</body>
</html>
